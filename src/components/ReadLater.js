import React from 'react';
import '../styles/App.css';

// On click, we will dispatch an action READING_LATER remove article from read later state, put it back into headlines, news and metadata, and mark as current with ACTIVATE_ARTICLE action (?)

function ReadLater() {
    return (
        <div className="App">
            <h4>Read Later</h4>
        </div>
    );
}

export default ReadLater;
