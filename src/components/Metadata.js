import React from 'react';
import '../styles/App.css';

// We update this section with current article data. We will allow users to favorite article here, and dispatch a STAR and UNSTAR actions; starts will show up in headlines section.

function Metadata() {
    return (
        <div className="App">
            <h2>Metadata</h2>
        </div>
    );
}

export default Metadata;
